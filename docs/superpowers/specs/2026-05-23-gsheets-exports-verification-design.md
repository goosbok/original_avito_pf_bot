# Verification of Google Sheets exports

**Дата:** 2026-05-23
**Автор:** Demyan Belikov
**Статус:** design

## Контекст

В `utils/googlesheets.py` четыре функции выгрузки данных в Google Sheets:

| # | Функция | Триггер в админке | Что выгружает |
|---|---------|-------------------|---------------|
| 1 | `create_sheet()` | callback `gsheets` в меню заказов ([handlers/admin_orders.py:280](../../../handlers/admin_orders.py)) | Все заказы (CSV → Drive → Sheets, батчи по 1000) |
| 2 | `create_orders_report(user_id)` | magic → пользователь → `orders` ([handlers/admin_orders.py:614](../../../handlers/admin_orders.py)) | Заказы конкретного юзера + его рефералов (вкладки) |
| 3 | `create_refills_report(user_id)` | magic → пользователь → `refills` ([handlers/admin_orders.py:635](../../../handlers/admin_orders.py)) | Пополнения юзера + рефералов (вкладки) |
| 4 | `create_reviews_report(orders)` | админка отзывов ([handlers/admin_reviews.py:244](../../../handlers/admin_reviews.py)) | Список отзывов |

После недавней миграции на ISO+UTC даты ([utils/dates.py](../../../utils/dates.py), коммиты `c473c8f`, `2493d33`, `45fe2fe`) три из четырёх функций используют `format_display(...)` для конвертации даты в `dd.mm.yyyy HH:MM MSK`. Цель — убедиться, что выгрузки не сломались, форматирование применяется, и большие объёмы данных (3к+ заказов) проходят через `create_sheet()` без OOM и за разумное время.

## Цели

1. Подтвердить, что все 4 выгрузки успешно создают Google-таблицы из реального Docker-окружения (prod-worktree, контейнеры `bot` и `api` уже запущены).
2. Убедиться, что после миграции дат отображение в каждой таблице — `dd.mm.yyyy HH:MM MSK`, а не ISO-строка.
3. Прогнать `create_sheet()` на синтетической нагрузке (3000 заказов), убедиться в стабильности по памяти и времени.
4. Зафиксировать найденные регрессии отдельными задачами (НЕ чинить в рамках этой работы — это работа по верификации).

## Не-цели

- Рефакторинг `utils/googlesheets.py`.
- Автоматизация (CI / pytest интеграционные тесты с реальным Google API) — оставлено на будущее.
- Покрытие краевых случаев Google API (квоты, ретраи, конкурентные запуски).
- Починка найденных багов в этой же ветке.

## Предусловия

- Docker `compose ps` показывает `bot` и `api` запущенными.
- Файл `utils/dev-trees-414317-e16633571d94.json` присутствует в основном worktree (verified — есть).
- Сервисный аккаунт имеет квоту на Google Sheets/Drive API.
- В БД есть данные после миграции `migrate_dates_to_iso`: хотя бы 1 заказ, 1 пополнение, 1 отзыв.
- У админа есть доступ к боту в Telegram (для триггера хендлеров).

## Архитектура верификации

Верификация делится на 2 фазы:

### Фаза A — функциональная (через Telegram-админку)

Запуск каждой из 4 выгрузок реальным сценарием — нажатиями в боте. Проверка выходной таблицы глазами по чек-листу. Это покрывает полный путь: `хендлер → utils.googlesheets → Google API → ответ в чат → клик по ссылке`.

### Фаза B — нагрузочная (3к заказов на `create_sheet`)

Перед нажатием «📊 Google Sheets» в админке инжектим в БД 3000 синтетических заказов, помеченных маркером `user_name='LOAD_TEST_3K'`. Запускаем выгрузку. После проверки — чистим маркером.

## Компоненты

### B1. Скрипт инжектора — `scripts/seed_load_test_orders.py`

**Назначение:** одноразово вставить N (по умолчанию 3000) заказов в таблицу `orders` для нагрузочной проверки.

**Интерфейс:**
```
python scripts/seed_load_test_orders.py [COUNT] [--user-id ID]
```

**Поведение:**
- Если `--user-id` не передан → берёт первого юзера из `users` (sanity: должен существовать, FK constraint).
- Использует `utils.dates.now_iso()` для поля `date` (проверяет, что ISO-пайплайн доходит до экспортёра).
- Маркирует все строки полем `user_name='LOAD_TEST_3K'` для последующей очистки.
- Поля для разнообразия (cycle):
  - `status`: `Posted` / `Completed` / `Pending` (по модулю 3)
  - `contacts`: True / False (чередуется)
  - `price`: 100 + (i % 50) × 100 (детерминированно, без random для повторяемости)
  - `position_name`: один из `["3 дня/200ПФ", "7 дней/500ПФ", "14 дней/1000ПФ"]`
  - `links`: одна строка `https://avito.ru/test/ad_{i}` (multi-link не нужен для нагрузочного — он только раздувает кол-во строк на выходе и затрудняет интерпретацию метрик)
- Одна транзакция, `executemany`. Печатает: `[seed] inserted 3000 rows in N.NN sec`.

**Идемпотентность:** при повторном запуске просто добавит ещё N строк. Это OK — за чистоту отвечает скрипт очистки.

### B2. Скрипт очистки — встроен в тот же файл

```
python scripts/seed_load_test_orders.py --cleanup
```

Выполняет `DELETE FROM orders WHERE user_name='LOAD_TEST_3K'` и печатает кол-во удалённых строк.

### B3. Чек-лист проверки (документ в этом же PR)

Markdown-таблица «пункт / прошло / нет / коммент» — заполняется по ходу прогона. Сохраним в `docs/superpowers/specs/2026-05-23-gsheets-exports-verification-results.md` после прогона (создаётся в фазе writing-plans, не сейчас).

## Чек-лист функциональной проверки

### 1. `create_sheet()` — общий отчёт
- [ ] Кнопка-ссылка пришла в чат (не «⚠️ Ошибка»)
- [ ] Таблица открывается публично (anyone:writer)
- [ ] Имя файла: `Заказы-DD-MM-YYYY-HH-MM-SS`
- [ ] 9 колонок, заголовок серый/жирный/по центру
- [ ] Фильтр на заголовке включён
- [ ] Ширины столбцов применены (40/100/100/500/80/140/80/80/140 px)
- [ ] **Колонка «Дата» — `dd.mm.yyyy HH:MM MSK`** (не ISO)
- [ ] Колонка «Статус» переведена: `Размещён` / `Выполнен`
- [ ] Колонка «Контакты» — `Да` / `Нет`
- [ ] Записи из `get_report_exclude()` отсутствуют в таблице
- [ ] В логах `docker logs original_avito_pf_bot-bot-1` есть строки `🚀 / 📦 / ✅ / 📤 / 🎉`, нет traceback

### 2. `create_orders_report(user_id)` — заказы юзера
- [ ] У юзера с рефералами — несколько вкладок (одна на юзера + по одной на реферала)
- [ ] У юзера без рефералов — одна вкладка
- [ ] Колонка «Дата» — `dd.mm.yyyy HH:MM MSK`
- [ ] На юзере без заказов и без рефералов — ответ «⚠️ Пользователь не оставил заказов!», без падения
- [ ] Форматирование (ширины, фильтр, заголовок) применено к каждой вкладке

### 3. `create_refills_report(user_id)` — пополнения юзера
- [ ] Кол-во вкладок = 1 + кол-во рефералов
- [ ] **Колонка «Дата»**: ожидаемо рендерится ISO-строкой, потому что в [utils/googlesheets.py:331](../../../utils/googlesheets.py) `amount_date.append(refill['date'])` без `format_display(...)`. Если в таблице видна ISO-строка — фиксируем как баг (отдельный таск).
- [ ] На юзере без пополнений — ответ «⚠️ Пользователь не вносил деньги!»
- [ ] Форматирование применено

### 4. `create_reviews_report(orders)` — отзывы
- [ ] 8 колонок, форматирование, фильтр
- [ ] Колонка «Дата» — `dd.mm.yyyy HH:MM MSK`
- [ ] Колонка «Сервис» — русские названия из `data.config.services`
- [ ] Колонка «Статус» переведена
- [ ] Записи из `get_report_exclude()` отфильтрованы

## Чек-лист нагрузочного прогона (фаза B)

1. [ ] `docker exec original_avito_pf_bot-bot-1 python scripts/seed_load_test_orders.py 3000` — отработал, напечатал `inserted 3000 rows`
2. [ ] `SELECT COUNT(*) FROM orders WHERE user_name='LOAD_TEST_3K'` → 3000
3. [ ] В отдельном окне запущен `docker stats original_avito_pf_bot-bot-1`
4. [ ] В админке нажать «📊 Google Sheets», засечь время от нажатия до получения ссылки
5. [ ] Память контейнера `bot` за время прогона не превысила 1× исходного значения более чем на 200 МБ
6. [ ] В логах видны батчи: `Обработка заказов 0-1000 / 1000-2000 / 2000-3000` (есть исходные `existing orders + 3000 LOAD_TEST_3K`)
7. [ ] Таблица создана, открывается, форматирование/фильтр применены даже на большом листе
8. [ ] Даты `LOAD_TEST_3K` строк в таблице — `dd.mm.yyyy HH:MM MSK` (а не ISO `2026-…T…Z`)
9. [ ] Cleanup: `docker exec ... python scripts/seed_load_test_orders.py --cleanup` → удалил 3000 строк

## Ожидаемые риски и решения

| Риск | Митигация |
|------|-----------|
| FK constraint при инжекте (нет такого `user_id`) | Скрипт сначала проверяет наличие юзера, иначе exit 1 с понятным сообщением |
| Google API rate-limit при батч-апдейте 9 запросов на 1 файл | Не блокирующее: `create_sheet` уже использует batchUpdate; если упрёмся — фиксируем как баг для отдельной работы |
| Старый креды-файл с истёкшим сроком | Будет ясная ошибка в логе → фиксируем баг |
| Тест-юзер попал в `report_exclude` | Перед инжектом — проверка `get_report_exclude()` в скрипте: если ID юзера в списке, exit 1 |

## Тестирование (что считаем «прошло»)

- Все пункты чек-листа фаз A и B отмечены [x].
- Найденные баги (если есть) выписаны отдельным списком в файл `docs/superpowers/specs/2026-05-23-gsheets-exports-verification-results.md` (создаётся при прогоне, не в этой ветке).
- Cleanup скрипта подтверждён (`SELECT COUNT(*) FROM orders WHERE user_name='LOAD_TEST_3K'` = 0 после).

## Что не делаем в этой работе

- Не чиним `create_refills_report` (нет `format_display` на дате) — фиксируем как баг, отдельный таск.
- Не добавляем юнит/интеграционные тесты с моком Google API.
- Не трогаем хендлеры или UI.
- Не выносим креды-файл из репозитория (отдельная security-задача).

## Артефакты

- `scripts/seed_load_test_orders.py` — новый файл
- `docs/superpowers/specs/2026-05-23-gsheets-exports-verification-design.md` — этот документ
- `docs/superpowers/specs/2026-05-23-gsheets-exports-verification-results.md` — создаётся при прогоне (не в спеке)
